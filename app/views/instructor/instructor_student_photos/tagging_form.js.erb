$modal = $('.inst_student')
$modal.html('<%= escape_javascript(render partial: "tagging_form") %>');
$modal.modal();
setTimeout(function(){

$('.select2_sample2').select2({
  placeholder: "Tag Someone",
  allowClear: true
});
},5000);
