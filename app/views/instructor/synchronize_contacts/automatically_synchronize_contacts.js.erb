total_id=document.querySelector('.total_contact').id;
count='<%=@instructor_students.count%>';

$('#sync_contact_count').text(total_id-count);
$.ajax({
	url: '/instructor/automatically_synchronize_contacts',
	type: 'GET',
	dataType: 'script'
})

