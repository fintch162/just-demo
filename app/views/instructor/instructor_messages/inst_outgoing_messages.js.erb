$('.inbox-content').html('<%= escape_javascript(render partial: "inst_conversation_incoming_outgoing") %>');
$('.sms-heading').html('Outgoing');
$('ul.inbox-nav li.active').removeClass('active');
$('ul.inbox-nav li.sent').addClass('active');
$(function () {
        $('.pagination a').live("click", function () {
                $.get(this.href, null, null, 'script');
                return false;
        });
});